eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

      
spring:
  application:
    name: client3
  cloud:
    config:
      uri: http://localhost:8888
      
    discovery:
      enabled: true
      service-id: eureka-server
